import React from 'react';
import Header from '../components/Header';

const Home = () => {
    return (
        <div>
            <Header />
            <p>This is the home page.</p>
        </div>
    );
};

export default Home;
